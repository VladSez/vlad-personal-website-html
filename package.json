{
  "private": true,
  "dependencies": {
    "@supercharge/promise-pool": "3.2.0",
    "@vercel/edge": "1.2.1"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1",
    "@types/node": "22.10.5",
    "vite": "6.0.11"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e-ui": "playwright test --ui",
    "test:e2e-update-playwright": "pnpm exec playwright install",
    "update-deps": "pnpm upgrade --interactive --latest",
    "dev": "npx vite www --port 3000",
    "build": "npx vite build",
    "preview": "npx vite preview --port 3000"
  }
}
